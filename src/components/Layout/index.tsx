import styled from "styled-components";
import { Sidebar } from "../Sidebar";

const Container = styled.div``;

export const Layout = () => {
  return (
    <Container>
      <Sidebar />
    </Container>
  )
};
