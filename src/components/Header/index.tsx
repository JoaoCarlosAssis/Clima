import { MeasurementSelector } from "../MeasureSelector";
import { HeaderContainer } from "./styles";

export const Header = () => {
  return (
    <HeaderContainer>
      <MeasurementSelector />
    </HeaderContainer>
  );
};
