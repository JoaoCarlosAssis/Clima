import { MeasurementSelector } from "../MeasureSelector";
import { Container } from "./styles";

export const Header = () => {
  return (
    <Container>
      <MeasurementSelector />
    </Container>
  );
};
