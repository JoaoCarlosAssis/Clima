import { UnitsSelector } from "../UnitsSelector";
import { HeaderContainer } from "./styles";

export const Header = () => {
  return (
    <HeaderContainer>
      <UnitsSelector />
    </HeaderContainer>
  );
};
